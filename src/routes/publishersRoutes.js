import { Router } from 'express';
import PublisherController from '../controllers/PublisherController.js';
import paginator from '../middlewares/paginator.js';

const router = Router();

router.post('/editoras', );
router.get('/editoras', );
router.get('/editoras/:id', );
router.put('/editoras/:id', );
router.delete('/editoras/:id', );

export default router;